#pragma once

enum shades {
    UP,
    HALF,
    DOWN
};

enum season {
    WINTER,
    SPRING,
    SUMMER,
    AUTUMN
};

enum fanSpeed {
    OFF,
    LOW,
    MEDIUM,
    HIGH
};
enum windSpeed {
    CALM,
    BREEZY,
    WIND,
    STORMY,
    HURRICANE
};

enum lightsOn {
    OFF_LIGHT,
    ON_DIM,
    ON_NORMAL,
    ON_BRIGHT,
};

enum motionDetection {
    NO_MOTION,
    MOTION_DETECTED
};
enum windowOpen {
    CLOSED,
    TILTED,
    OPEN
};
enum heaterOn {
    OFF_HEATER,
    ON_HEATER
};

enum gasAlarmON {
    OFF_ALARM,
    ON_ALARM
};
enum gasLevel {
    LOW_,
    MEDIUM_,
    HIGH_,
    EXTREME_
};
enum weather{
    SUNNY,
    CLOUDY,
    FULL_CLOUD,
    RAINY,
    WINDY,
    SNOWY
};

enum lightLevel {
    DARK,
    DiM,
    NORMAL,
    BRIGHT,
    VERY_BRIGHT
};

enum lightTop {
    ON,
    OOFF
};
